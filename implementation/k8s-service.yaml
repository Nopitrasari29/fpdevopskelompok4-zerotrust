apiVersion: v1
kind: Service
metadata:
  name: taskflow-api-service
  namespace: taskflow-prod
  labels:
    app: taskflow-api
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
  selector:
    app: taskflow-api
